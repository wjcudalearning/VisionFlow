# AOI CV Based 週報

日期：2026-07-23（星期四）至 2026-07-29（星期三）

本週完成 YOLOX Detector 第一階段垂直接入，涵蓋 CPU reference、Recipe Designer、ONNX Runtime CUDA fallback、共享 session 治理、驗收與穩定性工具，以及 RTX 3090 自動化流程；另新增 Pattern 定位固定網格批量切圖工具及 PySide6 GUI。期間共完成 9 筆提交，異動 36 個檔案，新增 5,524 行、刪除 263 行（不含 ONNX 二進位模型行數）；完整測試套件由 151 項增至 190 項。週期末主程式版本仍為 VisionFlow AOI v1.1.1。

## 本週完成事項

### 1. YOLOX CPU reference 與模型治理

- 完成 YOLOX M0／M1 CPU reference 垂直接入：加入 checksum 保護的 model registry、ONNX Runtime CPU session、manifest-driven letterbox／NCHW 前處理、raw decode、class-aware NMS、座標還原與 `DetectorYolox`。
- Recipe、DetectorManager、Pipeline、繁中顯示名稱、結果 metadata 與 execution metadata 已接入既有 AOI 契約；合成 CLI smoke 固定產生 2 筆 NG，驗證模型輸出、NMS 與全圖座標映射。
- 新增固定輸出的 tiny ONNX fixture、reference Recipe 與模型文件。fixture 僅用於軟體契約測試，不代表可辨識真實缺陷的 production 模型。
- Recipe 僅保存 `model_id`；registry 固定模型 SHA-256、版本、class mapping、輸入尺寸、前處理與 decoder，模型遺失或 checksum 不符時拒絕推論。

### 2. Recipe Designer 與操作安全

- Recipe Designer 依已驗證 registry 動態建立模型下拉，工程模式提供信心門檻、NMS IoU、NG 類別、最大筆數與最小框面積，管理模式才顯示 backend、precision 與 class-agnostic NMS。
- 模型輸入尺寸與 class mapping 採唯讀顯示；遺失模型、SHA-256 錯誤或 backend 不支援時，以繁中 inline notice 顯示並阻止儲存。
- 動態 YOLOX 參數已納入 dirty tracking；NMS 明確固定為同類別較低分框在 `IoU > threshold` 時移除，等於門檻時保留。

### 3. ONNX Runtime CUDA、fallback 與共享 session

- 完成 YOLOX M3 軟體接入：新增 ONNX Runtime CUDA FP32 session、provider／active execution provider 驗證，以及 CPU／CUDA 分離 cache。
- CUDA session 明確停用 CPU execution provider fallback；`gpu.mode=auto` 遇到 provider 缺少、初始化失敗、OOM 或推論失敗時，會完整以 CPU 重跑，`gpu.mode=cuda` 則維持嚴格失敗。
- YOLOX 透過 ONNX Runtime 執行，不誤載傳統 CV 使用的 `visionflow_cuda.dll`；GUI 單張、batch 與 monitor 透過既有 execution session 共用 AI manager。
- 加入 bounded inference queue、LRU session cache、模型／backend invalidation、安全 `close()`、warm-up 與 pipeline AI metrics；本機 fixture warm-up 5 次後連續 1,000 次維持單一 session／load、輸出 deterministic，RSS 僅增加 307,200 bytes。

### 4. YOLOX 驗收、穩定性與打包

- 新增 `gpu/validate_yolox_ort.py`，可用相同 ONNX 模型比較 CPU／CUDA raw output、NMS 後類別、數量、座標與分數容差。
- 新增 production acceptance manifest 與 validator，輸出 precision、recall、mAP50、誤殺率、漏檢率、per-class 統計及含 background 的 confusion matrix，並拒絕將 `test_only` fixture 當作量產模型驗收。
- 新增 stability validator，記錄 warm-up、10／100／1,000 次 checkpoint、session/load 數量、determinism、RSS 與 backend 證據。
- CPU-compatible PyInstaller package 已重建並通過 smoke；包內共 288 個檔案，包含 YOLOX registry、ONNX fixture 與 reference Recipe，不含 CUDA DLL，EXE smoke exit 0。
- RTX 3090 workflow 已加入 YOLOX CPU／CUDA 等價、CUDA 1,000 次穩定性與可選 production acceptance manifest，相關 JSON 均會上傳為 artifact。

### 5. Pattern 定位固定網格批量切圖

- 新增獨立 `export_pattern_grid_tiles.py`，重用 production Template Anchor Grid 定位邏輯，可依 Recipe 或直接參數批次處理資料夾影像。
- 支援遞迴搜尋、Unicode 路徑、逐圖錯誤隔離、PNG 小圖輸出，以及包含來源、列欄、座標與匹配分數的 CSV manifest。
- 新增 PySide6 GUI，不載入影像預覽，提供輸入／輸出／Recipe／模板與完整網格參數；Recipe 載入後可回填修改。
- 切圖在背景執行，具執行中關閉保護與工作參數保存；原有 CLI 介面完整保留。

### 6. 文件與專案維護

- 更新 README、模型說明、Recipe 範例、依賴鎖定與 Todo，記錄 YOLOX 架構、CPU／CUDA 行為、驗收命令、fixture 限制及 Pattern 批量切圖使用方式。
- 延續星期四至星期三的週報週期，補齊上一週截至 7/22 的成果與限制，讓版本紀錄與實際提交一致。

## 驗證結果

- 截至 7/27，完整單元與整合測試共 190 項通過。
- `compileall`、CUDA source／ABI preflight 與 `git diff --check` 均通過。
- YOLOX 合成 CLI smoke 固定輸出 2 筆 NG；CPU fixture stability 經 warm-up 5 次後連續執行 1,000 次，session／load 數量、輸出與記憶體均維持穩定。
- YOLOX GUI、batch、monitor、Unicode 路徑、session 共用、fallback、strict CUDA、raw／NMS 容差、acceptance schema 與 package contents 均有自動測試覆蓋。
- CPU-compatible PyInstaller package 包含 registry、fixture model 與 reference Recipe，不含 CUDA DLL；EXE smoke exit 0。
- Pattern 批量切圖已通過 4-tile CLI 實際 smoke、工具 GUI offscreen smoke、主 GUI offscreen smoke與回歸測試。
- 本機只有 `CPUExecutionProvider`，因此本週通過的是 CPU、fallback、靜態與打包驗證，未完成真實 CUDA execution provider 驗收。

## 目前限制與風險

- YOLOX 目前只有固定輸出的 tiny fixture，尚未導入可辨識真實產品缺陷的 production 權重，也沒有人工標註 acceptance set；不可將 2 筆合成 NG 或 1,000 次穩定性視為量產準確率證明。
- 本機缺少可用的 `CUDAExecutionProvider`、`nvcc` 與 RTX 3090；CPU／CUDA raw、NMS、效能、VRAM 與長時間穩定性仍待實機驗收。
- 最新 RTX job `30037233901` 因 self-hosted runner 離線持續 queued，heartbeat `30041558647` 失敗；workflow 已完成不等於硬體驗收通過。
- TensorRT FP32／FP16、batch inference 與正式 GPU VRAM budget 尚未完成；必須先取得固定資料集的準確率與效能證據再決定是否投入。
- 傳統 CV 的五份 production Recipe 仍缺各一張 PASS／NG，共 10 個固定案例；既有 CPU／CUDA 全流程量產驗收風險未因 YOLOX 接入而解除。
- Pattern 批量切圖目前以自動測試與 4-tile synthetic smoke 為主，仍需使用實際產線模板、光源變化與大批量資料確認定位門檻、失敗隔離及輸出容量。

## 下週計畫

1. 取得 YOLOX production ONNX 權重、class mapping、Recipe 與人工標註 PASS／NG 資料，建立可追溯 acceptance manifest。
2. 在 RTX 3090 上安裝相容的 `onnxruntime-gpu`，執行 CPU／CUDA raw 與 NMS 等價、warm-up 5＋1,000 次穩定性、VRAM 平台及 error／OOM fallback。
3. 以 production acceptance set 計算 precision、recall、mAP50、誤殺率、漏檢率與 per-class confusion matrix，再據此決定 confidence／NMS production 門檻。
4. 比較 ONNX Runtime CPU／CUDA 的 cold／warm median、P95、吞吐、模型載入時間與端到端時間；只有效能需求與證據成立時才評估 TensorRT。
5. 使用實際產線 Pattern Template Anchor Grid 資料驗收批量切圖，確認 Unicode 路徑、遞迴批次、匹配失敗、CSV 座標與大量輸出行為。
6. 恢復 RTX self-hosted runner 與 heartbeat，重新執行 YOLOX workflow；同時補齊傳統 CV 五份 Recipe 的 10 個 production PASS／NG 案例。

## 代表性提交

- `b5b6211` Plan YOLOX detector roadmap
- `97653c2` Add YOLOX CPU reference detector
- `33a2383` Complete YOLOX Recipe Designer
- `a70dfd3` Add YOLOX ONNX Runtime CUDA fallback
- `56413e5` Complete YOLOX CPU delivery validation
- `ad40ebc` Add YOLOX RTX acceptance workflow
- `5e672a0` Add pattern-anchored batch tile exporter
- `274e885` Add GUI to pattern grid batch exporter
